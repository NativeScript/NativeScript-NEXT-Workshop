<Page navigatedTo="load">
	<GridLayout>
		<ListView items="{{ memes }}">
			<ListView.itemTemplate>
				<StackLayout>
					<Image url="{{ url }}" />
				</StackLayout>
			</ListView.itemTemplate>
		</ListView>
	</GridLayout>
</Page>